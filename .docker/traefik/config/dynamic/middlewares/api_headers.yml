http:
  middlewares:
    api_headers:
      headers:
        customRequestHeaders:
          Content-Type: "application/json"
          Accept: "application/json"
        customResponseHeaders:
          Content-Type: "application/json"