http:
  routers:
    keycloak:
      rule: "Host(`keycloak.myproject.com`)"
      entryPoints:
        - websecure
        - web
      service: keycloak_service
      # middlewares:
        # - redirect-to-https
      # tls:
      #   domains:
      #     - main: "keycloak.myproject.com"