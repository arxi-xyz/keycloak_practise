http:
  routers:
    laravel_back:
      rule: "Host(`backend.myproject.com`)"
      entryPoints:
        - websecure
        - web
      service: laravel_back_service
      middlewares:
        # - redirect-to-https
        - header-back1
        # - auth_middleware
      # tls:
      #   domains:
      #     - main: "back1.myproject.com"

  middlewares:
    header-back1:
      headers:
        customRequestHeaders:
          X-Project-Name: "laravel"